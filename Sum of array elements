pragma solidity ^0.8.20;
contract ArraySum {

    function sum(uint[] memory arr)
        public
        pure
        returns (uint)
    {
        uint total = 0;

        for (uint i = 0; i < arr.length; i++) {
            total += arr[i];
        }

        return total;
    }
}
