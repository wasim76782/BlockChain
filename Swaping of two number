pragma solidity ^0.8.20;

contract SwapNumbers {

    function swap(uint a, uint b) public pure returns (uint, uint) {
        uint temp = a;
        a = b;
        b = temp;

        return (a, b);
    }
}
